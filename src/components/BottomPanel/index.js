import React, { memo } from "react";
import { BottomPanelWrapper } from "./style";

const BottomPanel = memo(() => {
  return <BottomPanelWrapper>BottomPanel</BottomPanelWrapper>;
});

export default BottomPanel;
